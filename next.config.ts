/**
 * ABOUTME: Next.js 15 configuration
 * RESPONSIBILITY: Configure Next.js App Router, Server Components, and build settings
 */
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  typedRoutes: true,
};

export default nextConfig;
