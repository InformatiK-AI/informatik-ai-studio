/**
 * ABOUTME: PostCSS configuration for Tailwind CSS v4
 * RESPONSIBILITY: Configure PostCSS with Tailwind CSS plugin
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
